[package]
name = "aletheia-mcp"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Aletheia protocol dispatch + MCP stdio server for agent automation"
publish = false

[lib]
name = "aletheia_mcp"
path = "src/lib.rs"

[[bin]]
name = "aletheia-mcp"
path = "src/main.rs"

[dependencies]
aletheia = { path = "../.." }
