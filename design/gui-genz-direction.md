# Gen-Z GUI direction (on DESIGN_GUI.md)

Do not replace [DESIGN_GUI.md](../DESIGN_GUI.md) — re-skin it.

Keep: 3-region pro layout, trust channel, Why? provenance, keyboard-first,
decompile toggle, diff buckets, PatchSet story mode.

Visual: sharper display typography (not Inter), calm density, micro-motion on
nav/caret only, command palette ⌘K. No purple-glow AI chrome, no card soup,
no emoji status. Patch recipes as a first-class “story” panel.

Stack: protocol ([protocol/PROTOCOL.md](../protocol/PROTOCOL.md)) → MCP → TUI
proof → Tauri (or similar) thin shell. Engine stays zero-dep.
