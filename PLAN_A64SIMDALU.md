# PLAN_A64SIMDALU — Advanced SIMD vector ALU (a64 ceiling)

Staged fidelity residue after wave three. lame's remaining unknowns are
mostly `fmul.2d` / `fadd.4s` / `add`/`cmhi` vector forms. One slice;
decode+lift only; listing text remains a separate never-guess increment.
