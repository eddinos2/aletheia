# Aletheia open FLIRT-style corpus — common prologues / epilogues / stubs.
# Text interchange only (not proprietary IDA .sig). TAB separates pattern/CRC and name.
# Researchers: append CRC32\tNAME lines from your redistributable libs.

# --- x86-64 SysV / Windows-ish frames ---
55 48 89 e5	x64_frame_setup
55 48 89 e5 53	x64_frame_setup_rbx
55 48 89 e5 41 54	x64_frame_setup_r12
55 48 89 e5 48 83 ec	x64_frame_sub_rsp
55 48 89 e5 48 81 ec	x64_frame_sub_rsp_imm32

# CET / shadow stack
f3 0f 1e fa	x64_endbr64
f3 0f 1e fa 55	x64_endbr64_frame

# Hot-patch / MSVC-ish
8b ff 55 8b ec	x86_msvc_hotpatch_frame

# Leaf / epilogue
c3	x64_ret
f3 c3	x64_rep_ret
c9 c3	x64_leave_ret
5d c3	x64_pop_rbp_ret

# Common call stubs / PLT-ish shapes (pattern only — name is illustrative)
ff 25	x64_jmp_rip_indirect
e9	x64_jmp_rel32
e8	x64_call_rel32

# memset-ish / rep stos (very short — high false-positive; labeled heuristic)
f3 aa	x64_rep_stosb_heuristic
f3 ab	x64_rep_stosd_heuristic

# --- AArch64 ---
fd 7b bf a9 fd 03 00 91	a64_frame_setup
fd 7b be a9	a64_frame_stp32
fd 7b bd a9	a64_frame_stp48
ff 43 00 d1	a64_sub_sp_imm
c0 03 5f d6	a64_ret
ff 0f 5f d6	a64_retab
fd 7b c1 a8 c0 03 5f d6	a64_ldp_ret
1f 20 03 d5	a64_nop

# ADRP+ADD GOT-ish (common PIC prologue fragment)
?? ?? ?? 90 ?? ?? ?? 91	a64_adrp_add_heuristic

# Branch / BL
?? ?? ?? 94	a64_bl_heuristic
?? ?? ?? 14	a64_b_heuristic

# Placeholder CRC (no match unless prefix hashes equal)
00000000	unused_crc_placeholder
